---
export interface Props {
  toc: Array<{
    depth: number;
    slug: string;
    text: string;
  }>;
}

const { toc } = Astro.props;
---

<ul>
  {toc.map(({ slug, text }) => <li><a href={`#${slug}`}>{text}</a></li>)}
</ul>
