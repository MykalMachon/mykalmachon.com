---
import type { SearchResult } from 'minisearch';

interface Props {
  result: SearchResult;
}

const { result: { title, description, url } } = Astro.props;
---

<article class="search-result">
  <h2><a href={url}>{title}</a></h2>
  <p>{description}</p>
</article>
